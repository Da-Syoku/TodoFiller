# 予定の中断（ドパチル → スキマス）

ドパチルの `INTEGRATION.md` が定めるスキマス→ドパチルの経路に対する、**逆方向の1本**です。
ブロック画面の「押し切り」（1タップで数分素通り）を無くす代わりの出口として使います。

## 考え方

抜けること自体は許すが、**タダでは抜けさせない。**

押し切りは「バレずに、記録も残らず、勉強も減らずに抜けられる」ので制限が飾りになります。
中断は違います。中断した予定は **「できなかった」として後の空き時間に再配置され**、
サーバーに記録も残ります。逃げても勉強の総量は減りません。

同時に、**閉じ込め事故を防ぐ出口**でもあります。許可リストを間違えても、
中断すれば必ず抜けられます。

## 送り方

```kotlin
val intent = Intent("dev.togar.dynasched.action.ABORT_SESSION").apply {
    setPackage("dev.togar.dynasched")          // ← 必須
    putExtra("id", "sched_8842")               // StudySync が渡した窓の id をそのまま
}
sendBroadcast(intent)
```

`id` はスキマスが送った窓の `id` をそのまま返してください（`sched_` 接頭辞込み）。
素の数値でも受け付けます。読めない値は黙って無視します。

## 権限

スキマス側に宣言済み:

```xml
<permission
    android:name="dev.togar.dynasched.permission.CONTROL_SESSION"
    android:protectionLevel="signature" />
```

ドパチル側のマニフェストに必要:

```xml
<uses-permission android:name="dev.togar.dynasched.permission.CONTROL_SESSION" />
```

`signature` 保護なので、**同じ鍵で署名したアプリからしか送れません。**
勝手に予定を消されないためです。両方とも `~/.android/debug.keystore`
（SHA-256 `827020ec...`）で署名しているので通ります。

## 受けたあとの動き

1. `POST /schedule/:id/skip {reschedule:1}` を投げる → サーバーが現在時刻以降に再配置
2. 端末内キャッシュからその窓を落とす
3. 残りの窓を `SYNC_STUDY_WINDOWS` で送り直す → **ドパチル側の制限がその場で解ける**
4. その予定の終了アラームを取り消す

**通信できなくても 2〜4 は必ず実行します。** ここで諦めると
「圏外だと中断できない＝閉じ込められる」ことになるためです。
サーバーへの反映だけが落ちるので、次にオンラインで予定を取り直したときに整合します。

## 壊れ方

| 起きること | 結果 |
|---|---|
| スキマスが圏外 | 制限は解ける。サーバーへの記録だけ落ちる |
| スキマスが未インストール | ブロードキャストは消える。窓は終了時刻で自然に解ける |
| 鍵が違うアプリから送られた | 届かない |
| 存在しない id | 無視 |
