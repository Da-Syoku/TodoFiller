# dist — 配布用ブランチ

スキマスの**最新版のAPKだけ**を置く枝です。ソースは `main` にあります。

- `skimas<版>.apk` — 最新版（例: `skimas34.apk`）。**版ごとに名前が変わります**
- `version.json` — アプリの「更新の確認」が見に来るファイル。`url` が上のAPKを指す

**この枝は版を出すたびに履歴ごと作り直します（force push）。**
普通にコミットを重ねると、古いAPKが5MBずつ履歴に永久に積み上がるためです。
ここに過去の版は残りません。
