# スキマス 使い方マニュアル

スキマスは、**Googleカレンダーに書いた「空き時間」へ、勉強と用事を自動で配置する**アプリです。
アプリ側で細かく時間を指定する必要はありません。カレンダーに「この時間は作業できる」と書いておけば、
スキマスがそこへ勝手に予定を入れます。

---

## 1. いちばん大事な仕組み — カレンダーのタグ

スキマスは Googleカレンダー（primary）の**予定タイトルの末尾**を見て、空き時間を判断します。

### 作業できる時間を宣言する

| 書き方 | 意味 |
|---|---|
| `自習室 **家**` | この時間は**家で**作業できる |
| `電車 **外**` | この時間は**外出中でも**作業できる |
| `部活` | タグ無し ＝ **予定あり。ここには何も入らない** |

- 末尾に「家」か「外」を付けた予定＝**作業できる枠（ウィンドウ）**
- タグを付けていない普通の予定＝**埋まっている時間**
- 旧記法の `&h`（家）・`&o`（外）も使えます

### 「家」と「外」の使い分け

タスクごとに「家じゃないとできない」「どこでもできる」を設定できます。

- **家** のウィンドウ → 家用のタスクも、どこでもできるタスクも入る
- **外** のウィンドウ → どこでもできるタスク・外でもできるタスクだけ入る

単語帳は電車で回せるけど問題演習は机が要る、といった使い分けができます。

### 重なった場合

タグ付きウィンドウとタグ無しの予定が重なると、**重なった部分は消えます**。

```
09:00 ────────────────── 12:00   「自習室 家」   ← 作業できる枠
          10:00 ─── 11:00        「歯医者」      ← 予定あり
結果:  09:00-10:00 と 11:00-12:00 にだけタスクが入る
```

### 終日予定の扱い

- **タグ無しの終日予定**（祝日など）は「埋まっている」扱いに**しません**。祝日で全部潰れることはありません
- **タグ付きの終日予定**（例：`終日 家`）は、起床時間帯 **6:00〜23:00 全体**が作業枠になります

### `%` が付いた予定

末尾に `%` が付いた予定は、**スキマスが自動生成したもの**です。
同期のたびに全部消えて作り直されるので、手で編集しても次の同期で消えます。触らないでください。

### テスト期間

タイトルが **「テスト期間」で始まる**予定（終日・複数日OK）を作ると、その期間は特別モードになります。

- **「定期テスト」をONにした教材だけ**が配置されます
- 単発タスクは停止します
- 漢字テストのような単発の小テストは「定期テスト」をOFFにしておけば、期間中に出てきません

### 注意点

- **「実家」「田舎の家」のように、たまたま末尾が「家」になる予定は作業枠として扱われます。**
  意図しない場合は「実家に帰る」のように末尾を変えてください
- 教材の名前の末尾を「家」「外」「%」にするのは避けてください。カレンダー書き込み後の同期で誤認識する可能性があります

---

## 2. 画面の説明

### 今日タブ

その日に配置された予定の一覧です。終わったら**「完了」**を押します。

通信できない時は**前回取得した内容をそのまま表示**します（ヘッダーに「オフライン表示」と出ます）。
電車の中でも今日やることは確認できます。

**「暇なとき」ボタン**を押すと、いまの状況を入れてやることを提示させられます。

- 場所（家 / 外）と、使える時間を入力するだけ
- 時間の初期値は「次の予定が始まるまで」が入っているので、そのまま押しても使えます
- 候補は**全部**出ます。決められない時は**「ランダム」**を押すと1つ選んでくれます
- 選ぶと、**終わっているはずの時刻に「できましたか？」と確認通知**が出ます
  - 「できた」→ 単発タスクなら完了、教材なら入力した問数を記録
  - 「できなかった」→ 何も記録しません。再配置もされません
- カレンダーの予定としては登録されません

### 単発タブ

一回やったら終わりのタスク（買い物、提出物など）を管理します。

| 項目 | 説明 |
|---|---|
| 名前 | タスク名 |
| 所要時間 | ホイールで時間・分を選択 |
| 優先度 | 高いほど先に配置される |
| 場所 | どこでも / 家 / 外 |
| メモ | 自由記述 |
| 色 | カレンダー上での色 |

親タスクの下に子タスクをぶら下げることもできます。

### 教材タブ

ワークや過去問など、**問題を数えて周回する**ものを管理します。
上部のカレンダーはスワイプで畳めます。

**入れるのは数えられるものだけです。**総時間・進捗%・難易度・理解度はもう聞きません。

| 項目 | 説明 |
|---|---|
| 教科 / 教材名 | 「物理」「ワーク」 |
| 総問数 | 試験範囲の問題数 |
| 応用の番号 | `19-22, 45-48` のように書く。**足りない時ここから自動で外れる** |
| 目標周回 | 何周するか（浅く何回も） |
| 試験日 | この日までに終わらせる |
| 1周目の締切 | 別に決められる。**過去問を間に合わせるための本命の締切** |
| 前提 | 「これが1周終わるまで出さない」（過去問←ワーク） |
| 種別 | **暗記** or **演習** |
| 必要なもの | どこでも / 机 / 声 / PC。**外の枠に置けるのは「どこでも」だけ** |
| 1コマ | 1回あたりの長さ（既定50分） |
| 定期テスト | ONにするとテスト期間中に優先される |

#### 「間に合う / 間に合わない」

各教材に**過不足が分数で出ます**。

```
化学 ワーク   1周目 12/95問 ・ 実測 4.2分/問
間に合わない（不足 1時間36分）
応用8問を外した → 2周に減らした
```

**足りないときは自動で削ります。**順番は **応用を外す → 周回を減らす**。
削った内容は必ず表示されるので、黙って消えることはありません。

#### 記録のしかた

聞かれるのは**「何問やった？」だけ**です。所要時間は聞かずに実測します。

- 予定の終了通知から、**通知の入力欄に直接**打てます（アプリを開かなくていい）
- 教材タブの**「記録」**からも入れられます。間違えたら「直前を取り消す」
- 問数を入れずに「完了」を押した場合は、コマの長さと実測ペースから推定して入ります

2〜3回記録すると**実測ペース**が出て、それ以降の見積もりが実測に切り替わります。
それまでは `3分/問（仮）` のように「（仮）」と表示されます。

**試験日はGoogleカレンダーにも自動で書き込まれます。**

### 単発タスクタブ

右上の**「並び順」**ボタンで、並べ方と見せ方をまとめて選びます。

- **並び順** — 手動 / 追加が新しい順・古い順 / 優先度が高い順 / 必要時間が長い順・短い順
- **完了したもの** — 「下にまとめる」か「その場に薄く残す」かを切り替え
- **タグで絞り込む** — 選んだタグの付いた枝だけを表示。
  タグの無い親も、子が当てはまれば残ります（木の形を崩さないため）。
  絞り込み中はボタンに `#タグ名` が出ます
- **すべて畳む / すべて開く**

### タグ

タスクの追加・編集画面の「タグ」欄で付けます。空白かカンマ区切りで、`#` は
付けても付けなくても同じです。「選ぶ」を押すと**これまで使ったタグ**から選べます
（「買い物」と「買物」が別物になるのを防ぐため）。

タグは2か所で効きます。

- **タスク一覧の絞り込み**（上記）
- **「暇なとき」の候補の絞り込み** — 「タグで絞る」を押すと、そのタグの付いた
  単発タスクだけが候補になります（絞っている間は教材は出ません）

親タスクは行頭の **▾** で畳めます。畳むと配下は消えて、親1行に
「3件（済1）・1時間20分」のような要約が出ます。

**行を長押しすると、そのブロックが持ち上がってドラッグで動かせます。**
モードに入る手順はありません。長押し＝つかむ、離す＝そこに置く、です。

- **上下** … 好きな位置へ移動
- **右へずらす** … 1段深くなり、上のタスクの**子タスクになる**
- **左へずらす** … 段が浅くなる。0段まで戻せば最上位
- つかんでいる間、画面上部に「いま何段目に置こうとしているか」が出ます
- **優先度順で並べている時は、落とした先の優先度になります**
  （優先度は低・中・高の3段なので、「どの塊に入れたか」が答えになります）

追加日時順や必要時間順で並べているときに動かすと、その並びを残すため
**手動の並びに切り替わります**（そうしないと次の描画で戻ってしまうため）。

### 設定タブ

- いまの動作状況
- **何日先まで埋めるか**の設定（既定7日）
- **スケジューラ実行** — 今すぐ配置し直してカレンダーへ反映。
  結果を数字で出す（枠の件数・空き分数・置いた数・カレンダーへの追加削除数）。
  1件も置けなかった時は**その理由**まで出るので、設定の問題か不具合かをここで見分けられる
- **通知の再設定** — 通知が来なくなった時に押す
- **カレンダーの読み取りを確認** — 何が枠として読めているかを一覧で見る。
  「調べる」から次の2つができる
  - **書き込みテスト** — 実際に1件書いて・読み直して・消す。
    予定が増えない／消えない時に、権限の問題なのかを確定させる
  - **他のカレンダーに残った自動生成を探す** — 昔のサーバー版は専用カレンダーへ
    書いていたので、いまも残っていることがある。件数を出して、消すかを選べる
- **起きている時間帯** — 起床と就寝を決める。アプリ全体で共有する設定で、3か所に効きます
  - この時間帯の外には予定を置かない（夜更かし前提の予定を作らない）
  - 就寝時刻に「今日はここまで」を通知（切れます）
  - 「暇なとき」の提案が就寝時刻で打ち切られる。残り90分を切ると残り時間も出ます
- **バックアップを書き出す / 復元** — 教材・実績・単発タスクをJSONファイルに書き出す。
  保存先は自分で選ぶ（クラウドのフォルダを選べば自動で控えが残る）。
  **アプリを入れ直すと端末内のデータは消えるので、これが唯一の控えです。**
  復元は全部入れ替えで、いま端末にあるデータは消えます
- **更新の確認** — 新しいバージョンがあれば案内


---

## 3. どう配置されるか

### 基本ルール

- 動く時間帯は設定した**起床〜就寝**（既定 6:00〜23:00）
- **18:00 を境に「昼」と「夜」**を分けています
- 15分未満の細切れには何も入れません
- 配置の優先順位は **単発タスク → 教材**

### 教材は「コマ」単位

教材は **1コマ＝50分**（変更可）を単位に置かれます。半端な切れ端には入りません。
1日に複数コマ入ることもあります。予定名には**やる問数**まで入ります（例：`物理ワーク 2周目 9問`）。

1日あたりの量はこう決まります。

```
1日の量 ＝ 残り時間 ÷ 残り日数（上限4時間、最低1コマ）

残り時間 ＝ 残り問数 × 実測ペース（分/問）
```

**難易度も理解度も使いません。**難しい教材は実際に遅いので、
それが実測ペースにそのまま出ます。申告する必要がありません。

### 周と周は間隔を空ける

**1周目 → 2周目は3日、2周目 → 3周目以降は7日**空けてから次の周に入ります。
連続で同じ教材を2周させないので、他の教科が押し出されません。
締切が近くて守れない時は詰めます。

### 前提条件

「これが1周終わるまで出さない」を設定できます（過去問 ← ワーク）。
ペース計算は**1周目の締切**に対して行うので、
「気づいたら直前で過去問の時間がない」が起きません。

### 暗記は夜、演習は昼

| 種別 | 昼(〜18:00) | 夜(18:00〜) |
|---|---|---|
| 暗記 | 入りにくい | **入りやすい** |
| 演習 | **入りやすい** | やや入りにくい |

暗記は寝る前が定着しやすいので夜に寄せています。

### どの教材が選ばれるか

```
スコア ＝ (優先度×2 ＋ 期日が近いほど加算) × 時間帯の相性
```

期日が30日を切ると、近づくにつれてスコアが上がっていきます。

---

## 4. 通知

### 開始時

予定の開始時刻に「実行中」通知が出ます。

### 終了時

終了時刻に、3つのボタンが付いた通知が出ます。

| ボタン | 動作 |
|---|---|
| **完了** | 終わったことを記録 |
| **何問やった？** | 通知の入力欄に問数を打つと、それが実績として記録される |
| **できなかった** | 予定を削除し、**後の空き時間へ自動で置き直す** |

**予定より早く終わった場合**、空いた時間に合うタスクを「次のおすすめ」として通知します。

### 通知が来ない時

設定タブの「通知を再設定」を押してください。端末を再起動した後は自動で復元されます。

---

## 5. ホーム画面ウィジェット

次の予定までの空き時間に合うタスクを表示します。

- **家 ⇔ 外**の切り替えボタン付き
- **「暇」**を押すとアプリが開き、場所と時間を入れて提案を受けられます（ランダム選択・確認通知つき）
- 30分ごとに自動更新、手動更新も可能

「今ちょっと時間空いたけど何やろう」を解決するためのものです。

---

## 6. どこで動いているか

**すべて端末の中で完結しています。** ログインもサーバーも要りません。

| | |
|---|---|
| 空き時間 | 端末のカレンダーを直接読む（Googleカレンダーが同期した内容） |
| 教材・実績・タスク | 端末の中（`skimas.db`） |
| 配置と「間に合うか」の計算 | 端末の中 |
| 圏外 | **全部動きます** |

以前はサーバーに問い合わせる方式もあり、設定で切り替えられましたが、
端末内で不足が出なくなったので消しました。

唯一の通信は**アプリの更新の確認**（設定タブの「アプリの更新を確認する」）だけです。
新しい版が出ていないかをGitHubに見に行くだけなので、圏外でも本体は普通に動きます。

**控えは端末の中にしかありません。** アプリを入れ直すと消えるので、
設定タブの「バックアップを書き出す」でファイルに残してください。
機種を変えるときも、書き出したファイルを新しい端末で読み込めば移せます。


---

## 7. アプリの更新

起動時に自動で新しいバージョンを確認し、あれば案内が出ます。
すぐ確認したい時は設定タブの「更新の確認」を押してください。
ダウンロードが途中で切れても、再開して続きから取得します。

---

## 8. 困った時

| 症状 | 対処 |
|---|---|
| 予定が全然入らない | カレンダーに「家」「外」で終わる予定を作っていますか。タグ付きの枠が無いと何も配置されません |
| 意図しない時間に入る | 末尾がたまたま「家」「外」になっている予定がないか確認してください |
| 通知が来ない | 設定タブの「通知を再設定」。端末の通知許可と、正確なアラームの権限も確認してください |
| 「ネットワークに接続されていません」 | 圏外です。今日タブは前回の内容を表示し続けます |
| カレンダーに勝手な予定がある | 末尾が `%` のものはスキマスの自動生成です。消しても同期で作り直されます |

---

## 付録：カレンダー記法の早見表

| 書き方 | 意味 |
|---|---|
| `〜家` / `〜&h` | 家で作業できる枠 |
| `〜外` / `〜&o` | 外でも作業できる枠 |
| タグ無し | 予定あり（配置されない） |
| `〜%` | スキマスの自動生成（触らない） |
| `テスト期間〜` | その期間は定期テストONの教材のみ |
| タグ無しの終日 | 埋まっている扱いにしない |
| タグ付きの終日 | 6:00〜23:00 全体が作業枠 |
